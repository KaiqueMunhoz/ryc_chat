import 'package:flutter/material.dart';

class MessagesBubble extends StatelessWidget {
  const MessagesBubble({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
